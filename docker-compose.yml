version: '3'

services:
    mongo:
        image: mongo
        restart: always
        ports:
            - "27017:27017"
        environment:
            MONGO_INITDB_ROOT_USERNAME: pmjames29@gmail.com
            MONGO_INITDB_ROOT_PASSWORD: philippians413